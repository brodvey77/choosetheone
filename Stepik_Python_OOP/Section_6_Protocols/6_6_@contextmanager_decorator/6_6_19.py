from contextlib import contextmanager


@contextmanager
def safe_write(filename):
    try:
        with open(filename, 'r') as file:
            old_content = file.read()
    except FileNotFoundError:
        old_content = None

    try:
        with open(filename, 'w') as file:
            yield file
    except Exception as error:
        if old_content is None:
            # Файл изначально не существовал — удаляем созданный файл
            import os
            os.remove(filename)
        else:
            # Восстанавливаем исходное содержимое
            with open(filename, 'w') as file:
                file.write(old_content)

        print(f'Во время записи в файл было возбуждено исключение {type(error).__name__}')




with safe_write('undertale.txt') as file:
    file.write('Тень от руин нависает над вами, наполняя вас решительностью')

with open('undertale.txt') as file:
    print(file.read())

Конечно. Разберём именно этот код буквально построчно, а заодно разберём, что происходит с @contextmanager, yield, try/except и почему файл удаётся вернуть в исходное состояние.

Вот код целиком:

from contextlib import contextmanager


@contextmanager
def safe_write(filename):
    try:
        with open(filename, 'r') as file:
            old_content = file.read()
    except FileNotFoundError:
        old_content = None

    try:
        with open(filename, 'w') as file:
            yield file
    except Exception as error:
        if old_content is None:
            import os
            os.remove(filename)
        else:
            with open(filename, 'w') as file:
                file.write(old_content)

        print(f'Во время записи в файл было возбуждено исключение {type(error).__name__}')
1. Импортируем contextmanager
from contextlib import contextmanager

contextmanager — это специальный декоратор из стандартной библиотеки Python.

Он позволяет написать контекстный менеджер в виде обычной функции с yield.

Без него пришлось бы создавать класс с методами:

__enter__()
__exit__()

А с @contextmanager всё гораздо проще.

Мы хотим в итоге использовать нашу функцию вот так:

with safe_write('file.txt') as file:
    file.write('Hello')
2. Создаём функцию
def safe_write(filename):

Создаём функцию safe_write.

Она принимает один аргумент:

filename

Например:

safe_write('undertale.txt')

Тогда внутри функции:

filename == 'undertale.txt'
3. Делаем функцию контекстным менеджером
@contextmanager

Это декоратор.

Он говорит Python:

Функция safe_write() будет использоваться как контекстный менеджер через with.

То есть благодаря ему можно написать:

with safe_write('undertale.txt') as file:

вместо обычного вызова:

safe_write('undertale.txt')
4. Первый try
try:

Здесь мы начинаем блок, в котором попробуем прочитать существующий файл.

Зачем?

Нам нужно запомнить первоначальное состояние файла.

Например, файл содержит:

Тень от руин нависает над вами, наполняя вас решительностью

Мы должны сохранить это содержимое в памяти.

Если потом внутри with произойдёт ошибка, мы сможем восстановить файл.

5. Открываем файл на чтение
with open(filename, 'r') as file:

Здесь файл открывается в режиме:

'r'

r = read — чтение.

Например:

with open('undertale.txt', 'r') as file:

означает:

Открыть undertale.txt для чтения и назвать файловый объект file.

Обратите внимание: здесь используется ещё один with.

То есть у нас есть внешний контекстный менеджер:

safe_write()

и внутри него ещё один:

open()
6. Читаем содержимое файла
old_content = file.read()

Метод:

file.read()

читает весь файл целиком.

Например, файл содержит:

Привет, мир!

Тогда:

old_content = file.read()

даст:

old_content == 'Привет, мир!'

То есть переменная old_content теперь хранит резервную копию первоначального содержимого файла.

Это ключевая часть решения.

7. Обрабатываем ситуацию, когда файла нет
except FileNotFoundError:

Что произойдёт, если файла вообще не существует?

Например:

safe_write('new_file.txt')

а new_file.txt отсутствует.

Попытка:

open(filename, 'r')

вызовет:

FileNotFoundError

Мы его перехватываем:

except FileNotFoundError:
8. Запоминаем, что файла не было
old_content = None

Если файла изначально не существовало, записываем:

old_content = None

Это будет нашим специальным признаком:

Файла до начала работы контекстного менеджера не существовало.

Это важно для последующего восстановления.

9. Второй try

Теперь начинается самая важная часть:

try:

Здесь мы уже непосредственно выполняем запись в файл.

10. Открываем файл на запись
with open(filename, 'w') as file:

Файл открывается в режиме:

'w'

w = write — запись.

Например:

with open('undertale.txt', 'w') as file:

означает:

Открыть undertale.txt для записи.

Важный момент

Режим 'w' очищает существующий файл.

Например, было:

AAAAAA

после:

open('file.txt', 'w')

файл становится пустым.

Поэтому мы до этого и сохранили его содержимое:

old_content = file.read()
11. yield file

Теперь самое интересное:

yield file

yield здесь передаёт управление пользовательскому коду, который находится внутри with.

Например, пользователь написал:

with safe_write('undertale.txt') as file:
    file.write('Тень от руин...')

Когда Python доходит до:

yield file

переменная:

file

передаётся сюда:

as file

То есть:

yield file

становится источником этого:

as file

Поэтому пользователь получает настоящий файловый объект.

И может делать:

file.write(...)
12. Что происходит во время yield

Представим:

with safe_write('undertale.txt') as file:
    file.write('AAA')
    file.write('BBB')

Выполнение примерно такое:

safe_write()
    ↓
сохраняем старое содержимое
    ↓
открываем файл для записи
    ↓
yield file
    ↓
передаём file пользователю
    ↓
file.write('AAA')
    ↓
file.write('BBB')
    ↓
выходим из with
    ↓
safe_write() продолжает работу

Именно поэтому yield в контекстном менеджере можно воспринимать примерно как:

«Вот тебе файл, работай с ним. Когда закончишь — я продолжу выполнение».

13. Если ошибки нет

Допустим:

with safe_write('undertale.txt') as file:
    file.write('Hello')

Никакого исключения нет.

Тогда:

yield file

возвращает управление в нашу функцию после завершения блока with.

Блок:

except Exception as error:

не выполняется.

Файл закрывается автоматически благодаря:

with open(...) as file:

И новые данные остаются в файле.

14. Что происходит при ошибке

Теперь рассмотрим:

with safe_write('undertale.txt') as file:
    file.write('Новые данные')
    raise ValueError

После:

file.write('Новые данные')

мы специально вызываем:

raise ValueError

Python создаёт исключение:

ValueError

И вот здесь происходит важная вещь.

Исключение возникает внутри блока with safe_write(...).

Поскольку yield находится внутри:

try:

исключение возвращается в нашу функцию и попадает сюда:

except Exception as error:
15. except Exception as error
except Exception as error:

Эта конструкция означает:

Перехватывать исключения, являющиеся экземплярами Exception, и сохранить объект исключения в переменную error.

Например, если произошло:

raise ValueError

то:

error

будет объектом исключения ValueError.

Можно условно представить:

error = ValueError()
16. Проверяем, существовал ли файл
if old_content is None:

Мы проверяем:

Файла изначально не было?

Если да, то раньше мы установили:

old_content = None

Поэтому условие:

old_content is None

будет True.

17. Импортируем os
import os

Модуль os нужен нам для работы с операционной системой.

В нашем случае — чтобы удалить файл.

18. Удаляем созданный файл
os.remove(filename)

Если файла до начала работы не существовало, а во время записи произошла ошибка, файл нужно вернуть в первоначальное состояние.

А первоначальное состояние было:

Файла вообще нет.

Поэтому мы его удаляем:

os.remove(filename)
19. Если файл существовал

Если:

old_content is None

ложно, значит файл существовал.

Например:

Старый текст

Тогда выполняется:

else:
20. Снова открываем файл
with open(filename, 'w') as file:

Открываем файл на запись.

Да, 'w' снова очищает его.

Но это как раз то, что нам нужно: сейчас мы собираемся полностью заменить его первоначальным содержимым.

21. Восстанавливаем старый текст
file.write(old_content)

Записываем обратно сохранённое содержимое.

Например:

до начала работы:

AAAA

мы сохранили:

old_content = 'AAAA'

потом пользователь сделал:

file.write('BBBB')

а затем возникла ошибка.

Получилось:

BBBB

Но мы выполняем:

file.write(old_content)

и снова получаем:

AAAA

То есть все изменения пользователя отменяются.

22. Выводим информацию об исключении

Остаётся:

print(
    f'Во время записи в файл было возбуждено исключение {type(error).__name__}'
)

Здесь используется f-строка.

Главная часть:

type(error).__name__

Разберём её.

Допустим:

error

является исключением:

ValueError

Тогда:

type(error)

даст класс:

<class 'ValueError'>

А:

type(error).__name__

даст только имя класса:

ValueError

Поэтому:

print(f'Во время записи в файл было возбуждено исключение {type(error).__name__}')

выведет:

Во время записи в файл было возбуждено исключение ValueError
Теперь разберём весь процесс на конкретном примере

Допустим, изначально:

undertale.txt

содержит:

Тень от руин нависает над вами, наполняя вас решительностью

Запускаем:

with safe_write('undertale.txt') as file:
    file.write('Новый текст')
    raise ValueError
Шаг 1

Сохраняем старый текст:

old_content = 'Тень от руин нависает над вами, наполняя вас решительностью'
Шаг 2

Открываем файл:

open('undertale.txt', 'w')

Файл очищается.

Шаг 3

yield file передаёт файл пользователю.

Шаг 4

Пользователь выполняет:

file.write('Новый текст')

Теперь файл:

Новый текст
Шаг 5

Пользователь выполняет:

raise ValueError

Возникает исключение.

Шаг 6

Мы попадаем сюда:

except Exception as error:
Шаг 7

Восстанавливаем:

old_content

Файл снова содержит:

Тень от руин нависает над вами, наполняя вас решительностью
Шаг 8

Выводим:

Во время записи в файл было возбуждено исключение ValueError
Шаг 9

И самое главное — исключение не пробрасывается дальше.

Мы его поглотили благодаря:

except Exception as error:

и отсутствию:

raise
Самая важная схема

Запомни логику этого контекстного менеджера вот так:

                 safe_write()
                      │
                      ▼
             Сохранить старый файл
                      │
                      ▼
              Открыть файл на 'w'
                      │
                      ▼
                 yield file
                      │
              ┌───────┴───────┐
              │               │
         ошибки нет        ошибка
              │               │
              ▼               ▼
       сохранить файл     восстановить
                          старое содержимое
                              │
                              ▼
                         вывести ошибку
                              │
                              ▼
                       поглотить ошибку
И ещё одна важная мысль

yield в данном коде — не просто способ вернуть значение. Он является границей между:

# подготовка контекста

и

# код пользователя внутри with

То есть:

try:
    # подготовились

    with open(...) as file:
        yield file       # ← здесь выполняется пользовательский with-блок

except Exception:
    # обработали ошибку пользователя

Именно благодаря этому safe_write способен перехватить исключение, которое пользователь вызвал внутри своего with.

Если хочешь разобраться именно в теме @contextmanager, следующим полезным шагом будет показать как этот же safe_write выглядел бы без декоратора — через класс __enter__ / __exit__, а затем
сравнить оба варианта построчно.